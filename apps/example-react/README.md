# React support reference

Runs the shared `supportChat` definition through the native React shell.

```bash
pnpm dev
```

See the [support reference guide](../../docs/examples/support-reference.md) for deterministic, provider, testing, persistence, and deployment modes.

Open `/?reference=onboarding` for the [deterministic onboarding reference](../../docs/examples/onboarding-reference.md).

Open `/?reference=operations` for the [policy-protected operations reference](../../docs/examples/operations-reference.md).

Open `/?reference=rag` for the [cited RAG reference](../../docs/examples/rag-reference.md).
